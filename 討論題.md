# 討論題
### 在你看來，機器人在執行料理任務的過程中，有哪些關鍵性的 Perception 資訊是機器人必須精確掌握的？你會如何設計系統或方法來實作並有效獲取這些 Perception 資訊？

在機器人料理任務中，不論是煎、炒、切、煮，我認為有四大類關鍵性 Perception 資訊是機器人必須精確掌握的。以下我會先從通用料理角度論述每類感知的必要性，再說明我會如何設計系統來獲取，並以本次 Take-home 的煎荷包蛋任務作為具體實作案例。

#### 一、食材狀態感知（Material State Perception）
料理的核心對象是食材，而食材在烹飪過程中會經歷劇烈的物理與化學狀態變化，機器人必須即時精確掌握：
1. **空間定位與邊界識別**：食材在工作區中的精確位置與幾何邊界。切菜時需要知道蔬菜在砧板上的位置與刀刃對齊關係；炒菜時需追蹤散落食材在鍋中的分佈；煎煮時需掌握液態食材（如蛋液、麵糊）受熱擴散後的動態邊界。
2. **物理狀態與相變識別**：食材的固/液/半固態與質地變化。例如肉類從生紅到焦褐、蛋白從透明液態到白色固態、蔬菜從硬脆到軟爛——這些相態轉換 (Phase Transition) 是決定下一步動作（翻面、起鍋、加水）的核心依據。
3. **溫度場與熱梯度**：利用熱成像感知食材的受熱分佈與溫度梯度，這是純 RGB 視覺無法獲取的隱藏維度。例如油炸時判斷內部是否熟透、煎肉時識別受熱不均的冷區。

**系統設計與實作方法：**
- **空間定位** → 採用即時語意分割模型（如 YOLO-Seg / SegFormer）對食材進行像素級邊界追蹤。關鍵設計決策：**將機器人末端工具（鍋鏟、刀具等）從視覺類別中剔除**，因為這些工具固定於機械臂法蘭盤上，可透過正向運動學 (FK) 與 ROS2 `/tf` 座標樹以 100% 精度取得位姿，不需浪費神經網路資源，反而能消除工具遮擋與反光造成的類別混淆。
- **狀態與相變** → 設計 RGB 可見光特徵（顏色/紋理變化）與 Thermal 熱梯度的**多模態融合**。可見光追蹤表面顏色質變；熱通道透過 Sobel 一階微分提取等溫線梯度 (∇T)，將溫度突變邊界以 HUD 方式注入 RGB 畫面，在不破壞自然色彩的前提下同時呈現熱力學資訊。
- **溫度場** → 將 RGBT 多通道感測器的第 4 通道即時解碼分離，並利用鍋具 mask 做空間約束，僅在有效工作區內計算溫度統計量，排除背景金屬反光等環境干擾。

> **煎蛋案例**：在本次 Take-home 中，我使用 YOLO11-Seg 微調 3 類別模型（wok/egg/container），結合 Method 3 Thermal Gradient Edge HUD，讓「低溫生蛋白」與「高溫鍋底」的邊界一目了然。

#### 二、烹飪環境感知（Cooking Environment Perception）
除了食材本身，機器人還必須感知整個烹飪環境的狀態：
1. **鍋具/容器幾何與狀態**：鍋具的精確輪廓、深度與傾斜角度，是否存在積油、焦屑或殘留物。這決定了工具的操作空間邊界與安全約束。
2. **熱源溫度與分佈**：鍋底/烤盤的溫度場是否達到目標值、是否均勻受熱。預熱不足會導致食材沾鍋，過熱則燒焦。
3. **食材與容器的空間交互**：食材與熱面的接觸區域及其溫度響應，是實現精細加熱控制的基礎（例如翻炒時確保每塊食材都接觸鍋底）。

**系統設計與實作方法：**
- **鍋具幾何** → 採用「深度學習動態預測 + 幾何先驗基底」的混合融合架構。當劇烈動作（如顛鍋、大幅傾斜）導致模型置信度下降時，幾何先驗自動兜底，並透過時序指數滑動平均 (EMA) 平滑輸出，確保 mask 穩定不跳閃。
- **熱源監控** → 在鍋具 mask 約束區域內逐幀計算 Thermal 通道統計量（均溫、峰值、梯度），映射至攝氏溫度後在 HUD 即時顯示，並作為 ROS2 Topic 發布供上位決策節點訂閱。
- **空間交互** → 計算食材 mask 與鍋具 mask 的交集區域 (Intersection)，同時利用不同容器之間的膨脹互斥約束，精確區分「備料區的食材」與「鍋中正在受熱的食材」。

> **煎蛋案例**：我實作了 YOLO-Seg + 幾何先驗圓的 Hybrid Fusion `/wok/mask`，並用 `container_mask` 膨脹取反做空間互斥，成功區分「碗中生蛋」與「鍋中受熱蛋體」。

#### 三、動作執行感知（Action Execution Perception）
機器人不只要感知環境，還必須感知自身動作的執行狀態與效果：
1. **工具-食材接觸狀態**：末端工具是否成功接觸食材、接觸力道是否適當。切菜時刀刃需精準對齊切割線；翻炒時鏟子需確實鏟起食材而非滑過表面；煎煮時需避免戳破脆弱的食材。
2. **食材位移與動作效果驗證**：執行動作後，食材是否如預期移動。翻面時食材是否成功翻轉、盛盤時食材是否成功轉移至目標容器。

**系統設計與實作方法：**
- **接觸狀態** → 工具末端位姿由機械臂運動學 + `/tf` 座標樹提供；視覺端專注感知食材的位移反應（mask 質心是否因施力而平移），形成「**運動學推斷施力 + 視覺驗證效果**」的互補閉迴路。進階系統可加入力/力矩感測器 (F/T Sensor) 做接觸力回饋。
- **動作效果驗證** → 連續追蹤食材 mask 的像素面積與質心坐標變化：面積突然歸零再恢復可判定為空中翻騰完成；質心大幅位移表示推移成功；食材脫離鍋具 mask 範圍則判定為起鍋完成。

#### 四、任務完成度感知（Task Completion Perception）
最上層的感知需求是對整體烹飪進度與成品品質的評估：
1. **食材熟度**：從生到熟是一個連續的不可逆物理化學過程（蛋白質變性、梅納反應、澱粉糊化），機器人需要一個量化的成熟度指標來精準控制烹飪時長。
2. **狀態機轉移與完成判定**：料理是一系列有序步驟（預熱→下料→加工→起鍋），每個步驟需要明確的進入/退出條件，才能觸發下一階段的動作。

**系統設計與實作方法：**
- **食材熟度** → 設計基於物理動力學的**不可逆熱量累積積分模型**：將成熟度建模為「超過該食材關鍵化學反應臨界溫度的有效熱通量對時間的積分」，並結合可見光顏色/紋理特徵做多模態加權。關鍵工程約束：嚴格單調不減 $D(t) = \max(D(t), D(t-1))$，因為烹飪變性是不可逆的化學反應，數值不應因遮擋或感測噪訊而回退。
- **狀態機** → 以形式化有限狀態機 (FSM) 組織料理流程，每個狀態轉移 **100% 由物理感知訊號門控**（溫度閾值、面積突變、熱量積分達標），實現**零硬編碼時間戳**的自適應控制。
- **全域監控** → 將所有感知模態整合於統一的多模態 Dashboard，以 ROS2 低延遲串流發布，供上位決策系統與人類監控者即時取用。

> **煎蛋案例**：我實作了 Arrhenius 動力學熱積分模型（$T_{crit}=60°C$，蛋白質變性臨界溫度）搭配 5 步驟 FSM（Pre-heating → Oil Seasoning → Side-1 Coagulation → Flip & Side-2 → Plating），以及 4-in-1 多模態 Dashboard，全程零硬編碼時間戳。


### 假設上述的感知模型已經訓練完成，且達到理想的準確度與可靠性，你接下來會如何將這個模型實際應用在機器人的料理任務流程當中？

感知模型訓練完成只是第一步，要真正落地到機器人料理任務中，我會從以下五個層面進行系統整合：

#### 一、建立感知驅動的閉迴路控制架構（Perception-Driven Closed-Loop Control）

機器人料理不能是「預先編排好時間表然後盲目執行」的開迴路 (Open-Loop) 系統，而必須是**感知訊號即時驅動決策與動作**的閉迴路 (Closed-Loop) 架構：

```
感測器串流 → 感知模型推論 → 場景狀態估計 → 決策/規劃層 → 動作執行 → 感測器串流 (循環)
```

具體而言：
- **每一幀感測器資料**經過感知模型推論後，產出結構化的場景狀態向量（食材位置、面積、溫度、熟度百分比等）。
- **決策層**接收這些狀態向量，根據當前狀態機 (FSM) 的階段與轉移條件，判斷是否需要觸發下一個動作（如翻面、加料、起鍋）。
- **動作執行**後，感測器立即捕捉執行結果，形成閉迴路驗證——如果翻面後發現食材沒有成功翻轉（mask 質心未改變），系統可以自動重試。

這樣的閉迴路架構使得系統能**自適應於不同食材、不同火力與不同鍋具**，無需為每道菜硬編碼時間參數。

#### 二、ROS2 節點化部署與 Topic 通訊設計

在 ROS2 架構下，我會將感知模型封裝為獨立的感知節點，透過標準化的 Topic 介面與其他子系統解耦：

| 節點 | 訂閱 Topic | 發布 Topic | 職責 |
|:---|:---|:---|:---|
| **感知節點** | 原始感測器串流 | `/perception/scene_state` | 模型推論 + 場景狀態估計 |
| **決策節點** | `/perception/scene_state` | `/control/action_command` | FSM 狀態轉移 + 動作決策 |
| **執行節點** | `/control/action_command` | `/robot/joint_trajectory` | 運動規劃 + 軌跡執行 |
| **安全節點** | 所有 Topic | `/safety/emergency_stop` | 異常監控 + 緊急停止 |
| **監控 Dashboard** | 所有 Topic | `/dashboard/visualization` | 人機介面 + 狀態視覺化 |

這種模組化架構的優點是：
- **可替換性**：感知模型升級時只需替換感知節點，不影響決策與執行層。
- **可測試性**：可以用錄製好的 ROS2 Bag 重播來離線測試決策邏輯，不需每次都操作實體機器人。
- **可擴展性**：未來加入新感測器（如深度相機、力感測器）時，只需新增對應的感知節點。

#### 三、即時性約束與模型部署優化

料理任務對延遲極為敏感——油溫在 200°C 時，0.5 秒的延遲就可能導致食材從完美焦香變成燒焦。因此需要嚴格的即時性保障：

1. **推論延遲預算**：整個感知-決策-執行迴路的端到端延遲應控制在 **≤ 100ms**（即 ≥ 10 Hz 控制頻率）。其中感知推論應佔 ≤ 50ms。
2. **模型輕量化**：
   - 選用輕量級架構（如 YOLO-Seg Nano/Small），避免使用過重的 Transformer 模型。
   - 部署時使用 TensorRT / ONNX Runtime 進行推論加速與 FP16 量化。
   - 若 GPU 資源有限，可採用**關鍵幀策略**：高頻（20 FPS）運行輕量級追蹤，低頻（5 FPS）運行完整分割模型，兩者融合。
3. **時序平滑**：感知輸出加入時序濾波（EMA / Kalman Filter），消除單幀噪訊跳動，讓下游決策層接收到穩定的狀態信號。

#### 四、FSM 狀態機驅動的任務編排（Task Orchestration）

我會用有限狀態機 (FSM) 作為料理任務的核心編排框架，**每個狀態轉移 100% 由感知訊號門控**：

```
[預熱] --鍋溫≥目標--> [下料] --食材入鍋檢測--> [烹飪] --熟度達標--> [翻面/調味] --完成驗證--> [起鍋裝盤]
```

每個狀態內部定義：
- **進入條件 (Entry Guard)**：必須滿足的感知前置條件（如鍋溫 ≥ 160°C）。
- **即時監控信號 (Monitoring Signals)**：持續觀測的感知量（如食材溫度、面積變化率）。
- **退出觸發 (Exit Trigger)**：觸發狀態轉移的感知閾值（如熟度 ≥ 85%）。
- **異常處理 (Exception Handler)**：感知到異常時的防護動作（如溫度過高時降火力、食材掉出鍋具時暫停）。

這種架構的核心優勢是**泛化性**——同一套 FSM 框架可以透過替換狀態定義與感知閾值，從煎蛋擴展到煎牛排、炒青菜等不同料理，無需重寫控制邏輯。

> **煎蛋案例**：在本次 Take-home 中，我實作的 5 步驟 FSM 完全由鍋溫閾值、蛋體吸熱冷斑、Arrhenius 熱積分達標等物理訊號驅動，零硬編碼時間戳。

#### 五、安全防護與人機協作監控

在實機部署中，安全性是最高優先級：

1. **多層安全防護**：
   - **感知層**：異常溫度檢測（乾燒警報 > 230°C）、食材掉落偵測（mask 突然消失）。
   - **決策層**：動作超時保護（如起鍋超過 15 秒未完成則重試或暫停）。
   - **執行層**：力矩限制、碰撞檢測、緊急停止 (E-Stop)。

2. **人機協作介面**：
   - 多模態 Dashboard 即時顯示所有感知狀態（RGB、Thermal、Mask、熟度曲線），讓人類監控者能一眼掌握全局。
   - 支援人工介入覆寫（如手動觸發翻面、調整目標熟度）。

3. **持續迭代機制**：
   - 部署過程中持續錄製 ROS2 Bag，自動收集模型失敗的 hard case（如新食材、新鍋具）。
   - 定期將 hard case 補充進訓練集進行增量學習，形成「部署 → 收集 → 重訓 → 再部署」的飛輪迴圈。


### 分享最近覺得很有趣的論文或技術文章，下面三個領域各一篇，說明它有趣在哪、你會怎麼用：

#### 1. Sim-to-Real (模擬到實機)
[Scaling Sim-to-Real Reinforcement Learning for Robot VLAs with Generative 3D Worlds](https://arxiv.org/pdf/2603.18532)
這篇論文解決了在真實世界中直接利用強化學習 (RL) 訓練視覺-語言-動作模型 (VLA) 時，模型容易過擬合且難以泛化的困境。其核心突破在於**結合語言模型與 3D 生成引擎（如 EmbodiedGen V2），全自動建構出具備豐富互動屬性的多樣化數位孿生世界**。配合為擴散模型設計的 PPOFlow 演算法進行大規模虛擬訓練後，模型在真實世界實機部署的成功率從 21.7% 大幅躍升至 75.0%，成功跨越了虛實鴻溝。

在機器人料理應用中，這項技術的價值在於能夠在虛擬環境中模擬出各種複雜的烹飪場景，如不同光照條件、食材表面紋理變化以及油煙干擾等。透過大規模的模擬訓練，機器人可以在虛擬環境中學習到如何在這些複雜環境下穩定地執行烹飪任務，從而大幅減少在真實環境中訓練所需的數據量和時間。此外，該論文提出的 PPOFlow 演算法能夠更有效地利用環境獎勵來優化模型性能，使得模型在真實環境中具有更好的適應性和泛化能力。

#### 2. Reinforcement Learning (強化學習)
[LaST-R1: Reinforcing Robotic Manipulation via Adaptive Physical Latent Reasoning](https://arxiv.org/pdf/2604.28192)
這篇論文挑戰了傳統離散文字「思維鏈」在處理高頻物理動態的缺陷。它創新地提出了「潛在推理優先」架構，並透過 LAPO 演算法讓環境獎勵直接重塑模型內部的物理推理表徵。其設計在極具挑戰的 LIBERO 基準測試中達到了 99.9% 的統治級成功率，展現了 RL 在連續控制與複雜物理推理上的強大潛力。

在機器人料理應用中，這項技術的價值在於能夠在虛擬環境中模擬出各種複雜的烹飪場景，如不同光照條件、食材表面紋理變化以及油煙干擾等。透過大規模的模擬訓練，機器人可以在虛擬環境中學習到如何在這些複雜環境下穩定地執行烹飪任務，從而大幅減少在真實環境中訓練所需的數據量和時間。此外，該論文提出的 PPOFlow 演算法能夠更有效地利用環境獎勵來優化模型性能，使得模型在真實環境中具有更好的適應性和泛化能力。

#### 3. Computer Vision (電腦視覺)
[ActiveSAM: Image-Conditional Class Pruning for Fast and Accurate Open-Vocabulary Segmentation](https://arxiv.org/pdf/2606.16996)
解決了基礎視覺模型在開放詞彙任務上的算力災難。它在不微調任何 SAM 3 權重的前提下，引入了基於影像條件的動態類別剪枝機制。透過提前過濾掉畫面中不存在的類別，它不僅達成了高達 5.5 倍的推論加速，更在八個基準測試中將平均 mIoU 提升了 1.4，完美達成了速度與精準度的平衡。

這篇論文的解決方案非常實用，尤其是在需要快速、準確地對未知物體進行分割的場景。我會將它應用在機器人料理任務中，利用它來快速分割不同的食材和鍋具，提高料理效率。同時，我也會利用它來輔助機器人的動作規劃，讓機器人能夠更精確地操作食材。